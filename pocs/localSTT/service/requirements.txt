# Silero VAD streaming turn detector. Pulls torch + torchaudio transitively.
silero-vad>=5.1,<7

# ONNX runtime for Smart-Turn v3 (used directly by our smart_turn.py wrapper).
onnxruntime>=1.18

# Smart-Turn v3 uses WhisperFeatureExtractor from transformers for mel feats.
transformers>=4.48,<5

# sherpa-onnx: SenseVoice-Small runtime. Ships aarch64 wheels on PyPI and
# includes its own ONNX Runtime — coexists cleanly with the top-level
# onnxruntime that smart_turn.py uses.
sherpa-onnx>=1.10

# WebSocket server (binary + text frames, async, minimal footprint).
websockets>=13,<14

# Numeric + audio I/O.
numpy>=1.26,<2.2
soundfile>=0.12

# In-process CPU + RSS sampling.
psutil>=7

# HuggingFace download helper used at build time by scripts/download_model.py.
huggingface_hub>=0.24
