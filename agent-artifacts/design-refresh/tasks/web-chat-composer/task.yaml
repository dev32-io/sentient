schemaVersion: 1
id: web-chat-composer
title: Build the Web ChatComposer and internal capture-aware VoiceCaptureControl
status: blocked
dependsOn:
  - web-foundation-components
  - capture-protocol-gateway
  - verification-inventory-harness
execution:
  resourceClaims: []
  assignment:
    agent: implementer
    tier: medium
    rationale: This is one behavior-rich but coherent product composite with
      deterministic fake-audio/state-machine proof.
assembly:
  stageId: product-surfaces
  intermediateState: complete
verification:
  timing: task
  methods: []
  taskChecks:
    - bun run --filter '@sentient/web-sdk' test:unit
    - bun run --filter '@sentient/webui' test:unit
    - bun run --filter '@sentient/webui' typecheck
    - bun run --filter '@sentient/webui' build
    - git diff --check
  rationale: Verify the contribution at its declared task boundary.
