# E2E evidence — session model & multi-surface (spec §10)

**Date:** 2026-08-03 · **Plan:** `docs/superpowers/plans/2026-08-02-session-model/` task 11 · **Branch:** `feature/native-orchestrator`
**Stack:** local dev only — `http://localhost:5173` (vite) → gateway `:8888`. Production (`mini0.lan` / `sentient.dev32.io`) was never contacted.
**User:** Ada (`u_0417d3b0`), PIN from the case library. **Model:** `deepseek-v4-flash:cloud` (ollama-cloud, free).
**Pre-flight:** `bun qa/web/stack-integrity.ts --log ~/.sentient/gateway/logs/2026-08-03.log` → `RESULT PASS — pass=9 fail=0` (re-run after the fault-armed restart: PASS again).
**Tool-call discipline:** reads and temp-writes only. The one confirm-tier tool exercised (`ma_queue`) was **denied** every time, so nothing executed. No `ha_call_service`, no `ma_playback` / `ma_play_media` / `ma_volume`.

**Artifacts:** `screenshots/`, `logs/gateway-log-excerpt-full-drive.txt` (144 unedited gateway lines spanning the whole drive), `logs/d21-stale-resume-fallback-cleared-console.log` (unedited browser console).

---

## Tally

**33 rows. 24 PASS · 4 FAIL · 3 BLOCKED (owner) · 2 PARTIAL.**

---

## How to read the "could the oracle have failed?" column

This branch has shipped four false greens, each because the check could not fail. Every green row below therefore carries an explicit falsifiability argument — usually a **control**: the same instrument, on the same code path, observed producing the opposite result during this drive. A row whose oracle could not have failed is marked PARTIAL and does not count as passed.

---

## Rows

| # | Case | Verdict | Oracle used | Could the oracle have failed? |
|---|---|---|---|---|
| 1 | `newchat-mints` | **PASS** | Wire, not the model: `stream-start messageCount` = **4** on the pre-"+" turn vs **2** on the post-"+" turn (system + the fresh user message only), and a newly-minted `sessionId` (`session.mint.created … firstMessageChars=96`). Secondary: a no-escape-hatch marker probe answered *"I don't have any marker string… this is the start of our conversation."* | **Yes.** The 2026-08-01 drive of this exact row recorded `messageCount` going 6 → 8 across the "+" click. The counter is server-side and cannot be talked around. The model-behaviour half is corroborating only — see row 9, where the identical probe string recited the marker verbatim. |
| 2 | `newchat-idempotent` | **PASS** (unit/integration, as the spec designates) | `gateway/src/session-handlers/session-id.test.ts` — *"INVARIANT: retrying the first message with one mint key yields one session"* + *"the retry is reported as replayed"*. 11/11 pass with `store/session-metadata.test.ts`. | Yes — the mint key is a `UNIQUE` column; task 2's ledger records the CAS test being rebuilt precisely because its first cut could not fail on its own bug. |
| 3 | `newchat-not-on-launch` | **BLOCKED** (mobile-only row) | — | Not driven. Requires Maestro against a simulator/emulator; this task drove web. On mobile the "+" and the app launch are **the same call**, which is why D15 is struck web-only. Carried with the D15 mobile half. |
| 4 | `draft-leaves-no-ghost` | **PASS** | `select count(*) from sessions` = **22 → 22** and `entries` 548 → 548 across opening a tab and closing it without typing; zero rows carrying that draft key; log shows `session-configure.draft.fresh` and no `session.mint.created`. | **Yes.** A ghost increments the table; the count is read straight from SQLite, independent of any UI. |
| 5 | `title-appears` | **PASS** | Both windows' frame taps received `session.title {title:"Sourdough starter feeding", sessionId:s_f9e726f3…}`, and both drawers re-rendered the new title without a refresh. B had sent nothing. | **Yes.** A connection-lane or single-window title would leave B's tap without the frame. B's tap is proven live by rows 11/12. |
| 6 | `title-rename-wins` | **FAIL** | `PATCH /api/v1/sessions/:id` with Ada's own token → **404 `{"error":"not-found"}`**. The route does not exist, so `provenance="user"` has no producer in production and the CAS guard cannot be reached from the product. | n/a — this is the failure. Filed as a follow-up in `docs/native-todo.md`; **deliberate ordering**, per plan task 10's reviewer (guard before producer). Not a regression. |
| 7 | `title-fallback` | **PASS** (`fault-armed`) | Armed by moving `gateway/system_prompts/auxiliary/title.md` aside and restarting (the loader memoises per process). Log: `auxiliary-template-missing` → `auxiliary-task.failed reason="template-missing"` → `titler.generation-failed … fallback="truncated-first-message"` → `titler.applied titleChars=60 fallback="truncated-first-message"`. Title shown: *"What temperature should a sourdough starter be kept at over…"* — readable, never "Untitled". | **Yes.** Two `titler.applied … fallback=null` lines from the healthy path sit in the same log minutes earlier. Arming reverted; `git status` clean. |
| 8 | `sessions-list` | **PASS** | `GET /api/v1/sessions` → 200, 22 rows, newest-updated first, drawer groups Today/Yesterday, no error banner. Scoping is row 24. | Yes — the drawer read "No past chats yet." before the fetch resolved, so the populated state is a real transition. |
| 9 | `session-resume` | **PASS** | Reopened `s_b92ccc82…`: rendered 4 bubbles = the store's 2 user + 2 assistant. Follow-up turn `stream-start messageCount=6` (full prior context) and the model recited `ZEBRA-7741-KETTLE` **verbatim** to a no-hatch probe. `conversation.activate.switched` logged; attach generation 1 → 2. | **Yes, and it is the control for row 1.** The identical probe string, minutes earlier, produced "I don't have any marker string" in the freshly-minted session. Same words, opposite answers, discriminated by which session was attached. |
| 10 | `legacy-session-opens` | **PARTIAL — addressable, but not listable** | Raw-wire `session.configure` with `c::u_0417d3b0::6e2aad99-…` → `session.attached generation=1` + `conversation.snapshot items=138`; log `session.resolve.legacy-partition … reason="no metadata row, but the caller's store holds entries for this id"`. No prefix parse, membership by store contents. | The *addressability* oracle could fail (row 21 shows the same probe refusing ids). But the spec row says **"open it from the list"**, and that is unsatisfiable: `GET /api/v1/sessions` reads the `sessions` metadata table, which legacy partitions have no row in. Ada has **7 legacy partitions / 372 entries** invisible in the drawer. Counted PARTIAL, not PASS. |
| 11 | `two-windows-live` | **PASS** | B sent **zero** `text.input` frames yet received `turn.started`, 9 × `turn.text.delta`, `turn.completed`, `conversation.entry`, `turn.audio.start`, binary Opus frames and `turn.audio.done` — all on one `turnId`, reassembling to the full reply. Exactly one `react-loop.start` for that turnId. `subscribers=2`. | **Yes.** The pre-wave evict-on-claim registry would have torn A down on B's attach, leaving B with a private turn and A with nothing. `sentAnything:0` rules out B driving it itself. |
| 12 | `two-windows-steer` | **FAIL (mechanism PASS, outcome wrong)** | Seam is correct: `session-runtime.submit.steer … seq=560 turnId=f61918ce`, the store shows seq 560 appended **under the running turn's `turn_id`**, no second `react-loop.start`, no `session_busy`. But the steer was never acted on — the turn answered the original prompt, and the follow-up turn committed *"Sorry — something went wrong while I was answering."* | **Yes**, and it did: the row's stated expectation ("folded into the running turn") is only half met. Reproduced with a **single** window, so it is not multi-window-specific. Filed as **D22**. |
| 13 | `two-windows-race` | **PASS** | Both composers fired via `Promise.all` (45 ms apart). `input-arbiter.busy … attachmentId=<B> holderAttachmentId=<A> elapsedMs=16 windowMs=100` + `command.refused reason="session_busy"`; exactly one `react-loop.start`. User-visible: B's toast *"Someone else was sending at the same moment — try again."* | **Yes.** `elapsedMs=16` proves the race was inside the 100 ms window — a sequential drive lands outside it and *steers* instead, which is a different observable. **Correction worth recording:** an initial reading at +8 s found no toast and I nearly filed a silent-data-loss defect; re-sampling at 200/600/1500/3000 ms showed the toast lives ~1.5–3 s. The first oracle was mistimed, not the product. |
| 14 | `join-midturn` | **PASS** on turn-state; audio half **not exercised** | `fan-out.turn-state-sent … turnId=c6b392bd… trigger="user" textLength=0 runningTools=1 openPrompts=0 audioTurnId=null` then `fan-out.attached … committedFeed="client-refetch"`. B rendered the in-flight turn and showed an enabled **Interrupt** button. | The turn-state half: yes. The `audio.skip-reason="midturn-join"` half **could not have fired** — `audioTurnId=null`, i.e. no audio was in flight at the join (the turn was still doing tool calls; TTS starts after the text settles). Recorded as not exercised rather than counted. |
| 15 | `join-race` | **FAIL** | Store: **4** `tool_call` entries (3 × `search_web`, 1 × `fetch`). Long-attached window A renders **4** `.tool-pill` nodes; mid-turn joiner B renders **5**. | **Yes** — A's count matching the store is the control proving the counting method is sound. Same class as the defect task 6 recorded as (b), reproduced on a tool pill. Filed as **D23**. Note: an `innerText` line-diff pointed the *wrong way* (the composer task strip mirrors pills as one grouped node); the `.tool-pill` count against SQL is the trustworthy oracle. |
| 16 | `permission-either-answers` | **PASS** | Prompt raised by A's turn fanned to **both** windows with the same `requestId`. Denied on B. `session-permission.request … windows=2 pending=1` → exactly one `session-permission.settled … reason="denied" attachmentId=<B> pending=0`, and `permission.resolved{denied}` reached A. | **Yes.** A second settle, or a settle naming A, would both show. Trap avoided: the dialog still looked open afterwards — it was a *different* `requestId` (the model retries after a deny), caught by reading the id rather than the pixels. |
| 17 | `permission-issuer-leaves` | **PASS** | A (whose turn raised prompt `c7ab28ca`) was closed; the prompt stayed live on B, B denied it, `permission.resolved{denied}` returned. Settle line carries `windows=1`. | **Yes.** A socket-scoped broker would have denied-all on A's close; `session-permission.deny-all` appears elsewhere in the same log for genuinely closing sessions, so the mechanism is observable. |
| 18 | `permission-timeout-denies` | **PASS** | Prompt `23f277fe` raised 03:24:48.539 with `expiresAtMs=1785752808538`, nobody answered, settled 03:26:48.540 — exactly 120 000 ms — with `reason="timeout"` and **`attachmentId=null`**. | **Yes.** The two answered prompts immediately above settle with `reason="denied"` and a non-null attachmentId. Fail-closed confirmed by contrast in one log. |
| 19 | `stop-from-either` | **PASS** | A's turn streaming; **B** pressed Interrupt. `[ws] interrupt.requested … attachmentId=at_ec86c4d5…` (B's attachment), one `cancellation.abort … cutoff="interrupt" cancelBackground=true`, and **both** windows received `turn.aborted:interrupt` + `playback.stop`. | **Yes.** B's attachmentId is distinct from A's, so mis-attribution would be visible; the fan-out of `turn.aborted` to A is what proves "stops for both". |
| 20 | `bargein-cutoff` | **BLOCKED — owner** | Needs a real microphone and real acoustics. See the handoff below. | — |
| 21 | `stale-generation-dropped` | **PASS** | Raw wire (unreachable through the Preact composer, which batches the input event): attach to S1 (gen 8), `conversation.activate` to S2 (gen 1), then land a `text.input` still stamped `{S1, gen 8}`. → `command.rejected reason="stale_generation"`, logged with both claimed and actual binding. **`select count(*) … text like '%STALE-9999%'` = 0** across the whole store. | **Yes.** Unbound, the text would have committed into S2 and the count would be 1. The assertion is against the database, not a pane. |
| 22 | `connection-lane-private` | **PASS** | All three frame classes forced on other connections to the same session — auth refresh (`auth.ok`), resume (`session.attached`, `session.ready`, `conversation.snapshot`) and `pong`. B's tap received **zero frames** over the whole window. | **Yes — this is the row that most needed a control, and it has one.** The identical tap on the identical socket recorded 9 deltas + audio in row 11. An empty array from a dead instrument would look the same; here the instrument is proven alive. |
| 23 | `last-one-out` | **PASS** | A closed → `session-binding.detached … subscribers=1`, **no** `session-registry.disposed` for that id, and B then completed a full turn (text + audio). | **Yes.** `session-registry.disposed` fires for two *other* sessions in the same log window (retention), so its absence here is meaningful rather than a check that never fires. |
| 24 | `unknown-session-refused` | **PASS** (`security`, driven) | `s_deadbeef…` → no `session.attached`; fresh `session.draft` + `conversation.snapshot items=0`; `session.resolve.unknown … reason="not present in the store this caller's capability opened — refusing to create it"` + `session-configure.session.refused`. | **Yes.** The same probe on the same code path returned `session.attached` + `items=138` for a legitimate legacy id (row 10) minutes earlier. That positive control is what makes the refusal meaningful. |
| 25 | `cross-user-refused` | **PASS** (`security`, driven) | Two **real, on-disk** partitions belonging to other users (`c::u_1eee01a4::16a4b56e-…`, `c::u_885ffeb7::step19-surface`) both refused identically; `items=0`, no data from either. Also `GET /api/v1/sessions/c::u_1eee01a4::…/messages` → 404 (`sessions.messages.not-found reason="unknown-session"`). | **Yes**, same positive control as row 24. Note the refusal line is *identical* to "unknown" — by design: another user's store is a different SQLite file the capability never opened, so "not yours" and "not there" are structurally indistinguishable. |
| 26 | `wrong-capability-refused` | **PASS** (`security`, unit as the spec designates) | `gateway/src/store/session-store.test.ts:75` — *"SECURITY: a capability for another resource class cannot open the session store"*. `bun test store/session-store.test.ts -t "another resource class"` → 1 pass. | **Yes, by construction:** the test grants a real `file-scope` capability from a real `AccessManager`, so it carries the **exact same `rootPath`** as the valid one — only the resource-class check can produce the throw, and `/resource class/i` pins the message. No mutation run: editing gateway source mid-drive restarts the watched gateway. |
| 27 | `expired-credential-refused` | **PASS** (`security`, driven, `fault-armed`) | Armed `auth.token_ttl_seconds: 60` (schema floor is 60; 45 fails zod at boot) + restart + fresh login. After the TTL: `command.refused … reason="credential_expired" detail="this connection's token expired — attachment dropped and socket closing"`, client got `command.rejected:credential_expired` then `auth.error:expired`, socket closed, **webui dropped to the login screen**, and `select count(*) … '%EXPIRED-PROBE%'` = **0**. | **Yes.** The identical command 90 s earlier, on a live token, produced a full turn and a committed entry. Config restored and gateway restarted; `stack-integrity` PASS, `git status` clean. |
| 28 | `userId`-in-request (task 4) | **PASS** (`security`, driven) | `GET /api/v1/sessions?userId=u_1eee01a4` with Ada's token returned a **byte-identical** body to the plain call (3476 = 3476), logged `userId="u_0417d3b0"` plus an explicit `sessions.userId-param-ignored … suppliedUserId="u_1eee01a4"` WARN. An `X-User-Id` header spoof was also byte-identical. No token → 401. | **Yes.** The named user has **zero** rows in the `sessions` table, so an honoured param would have returned `{"sessions":[]}` — a ~3 460-byte difference. |
| 29 | `retention-holds-work` | **PASS** | Approved a `delegateTask`, then closed **every** window while it ran. `session-retention.timer-armed kind="recheck" … reasons=hasUnfinishedBackgroundTask` — the exact reason the row demands. Hermes completed with zero windows (`hermes-runner.run.ok verdict="succeeded"`); no dispose. On reattach the result was present: *"The boiling point of nitrogen is -195.8 °C."* | **Yes.** Only after the work finished did `timer-armed kind="disposal" delayMs=900000 reasons=` (empty) arm — the two lines differ exactly in the held reason. |
| 30 | `retention-bg-followup` | **PASS** | `turn-emitter.turn-started … trigger="background-completion"`, `react-loop … textLength=47`, and the reattached window shows the **actual content**, not an acknowledgement — i.e. D16's fix holding on a turn nobody was watching. | **Yes.** This is precisely the shape that produced `completionTokens=1 textLength=0` on 2026-08-01. |
| 31 | `retention-drops-idle` | **PASS** | The sweeper disposed two idle sessions during the drive: `session-retention.disposing … retentionMs=900000 reason="the retention grace elapsed and nothing holds this session"` + `session-registry.disposed`. Reattaching to a disposed id gave `session.attached generation=1` (fresh runtime) + `conversation.snapshot items=2`, matching the store. | **Yes.** Driven against a session the sweeper had *already* disposed on its own clock, so the dispose was not manufactured. `generation=1` proves a rebuild rather than a survivor. |
| 32 | `retention-timer-race` | **PASS** (`fault-armed` → harness, as the spec directs) | `gateway/src/runtime/session-retention.test.ts` — *"INVARIANT: a timer that fired while a connection was attaching cannot dispose a live session"* **and** *"INVARIANT: disposal re-derives retention at fire time, not only its generation stamp"*. 16/16 pass. | **Yes**, and this is documented: task 8's ledger records that the plan's own attach test **passed under a generation-stamp-only fix**, so the implementer added the fire-time re-derivation counter-case. Driven as a harness test because wall-clock luck is not an oracle. |
| 33 | `reload-convergence` | **PASS on the first reload; bounded by D21** | Both windows reloaded simultaneously onto one session: both attached, both rendered 2 bubbles, feeds **byte-identical**. | **Yes** — but the row only holds once. A **second** reload of either window lands on a fresh empty draft (D21), and the feeds diverge completely. Recorded as PASS with the caveat rather than an unqualified green. |

### Also outstanding for the owner

| Case | Why the browser cannot reach it |
|---|---|
| `bargein-cutoff` | Needs a real microphone and real room acoustics. **Exact check:** speak over active TTS, then grep the gateway log for `[runtime:session-runtime] session-runtime.playback.stop … cutoff="barge-in"`. The discriminator against Stop is the *next* line: an interrupt logs `cancellation.abort … cancelBackground=true` and `cancellation.background.cancel-all`, whereas a barge-in must leave background tasks **running** — so with a `delegateTask` in flight, confirm no `background.cancel-all` fires and the delegation still completes. Both halves of the interrupt path are proven in row 19; only the acoustic trigger is missing. |
| `voice-roundtrip` | Outstanding from E2E round 2. Mic → STT → turn → TTS on real hardware. |

---

## New defects filed (all in `docs/native-todo.md`)

- **D21** — a window that reloads onto a session forgets it; the *second* reload loses the conversation. The stale-resume guard's own precondition comment is false for the `session.configure` path. Console evidence: `logs/d21-stale-resume-fallback-cleared-console.log`.
- **D22** — a steer landing after the final provider iteration is dropped, and the compensating follow-up turn commits a durable "something went wrong" bubble. Not multi-window-specific.
- **D23** — a mid-turn joiner renders one duplicate tool pill (5 for 4 real calls).
- **`PATCH`/`DELETE /api/v1/sessions/:id` absent** while `web-sdk`, the mobile `SessionsHttpClient` and `qa/mobile/flows/android/07-rename-delete.yaml` all still call them.

## Oracle failures caught during this drive (recorded so they are not repeated)

1. **A loose regex over `document.body.innerText`** reported "mentions busy/error: true" for the race row. Extracting the actual match found **zero** hits — the earlier `true` was a mistimed sample. Fixed by extracting and printing the matched substring instead of trusting a boolean.
2. **An `innerText` line-diff of two panes** appeared to show the *joiner* missing a tool pill. Counting `.tool-pill` nodes against the store showed the opposite (the joiner has an extra). The composer task strip mirrors pills as one grouped text node, so text-diffing panes is not a valid pill oracle.
3. **Nearly filed a silent-data-loss defect** on the race loser after sampling the UI 8 s post-action. The toast lives ~1.5–3 s. Sample transient UI on a schedule, not once.
