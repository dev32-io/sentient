appId: io.dev32.sentient.debug
tags: [helper, fault-armed]
---
# Restore the unavailable native network path before invoking. Calendar remains
# open from 75; this flow must not launch, login, navigate, or tap Retry.
- assertVisible: { id: "${CALENDAR_CACHED_EVENT_TAG}" }
- extendedWaitUntil:
    visible: { id: "calendar-recovery-ready" }
    timeout: 20000
- extendedWaitUntil:
    visible: { id: "calendar-freshness-up-to-date" }
    timeout: 20000
- scrollUntilVisible:
    element: { id: "${CALENDAR_RECOVERY_EVENT_TAG}" }
    direction: DOWN
    timeout: 20000
- scrollUntilVisible:
    element: { id: "${CALENDAR_CACHED_EVENT_TAG}" }
    direction: UP
    timeout: 20000
- assertNotVisible: { id: "calendar-freshness-offline" }
- assertNotVisible: { id: "calendar-freshness-stale" }
- assertNotVisible: { id: "calendar-freshness-refreshing" }
- assertVisible:
    id: "calendar-add"
    enabled: true
