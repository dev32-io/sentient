appId: io.dev32.sentient.debug
tags:
  - auth
---
# login — standalone auth test: cold-launch (clearState) → avatar select → PIN
# entry → chat screen. Proves the Koin DI + Navigation-Compose nav gate routes to
# chat on valid auth. The avatar+PIN body lives in _helpers/login.yaml (shared with
# every authed flow's conditional login); this flow forces the login screen via
# clearState, then delegates. PIN is 1234 (Kevin / u_8c866990).
#
# Also serves as the runner's setup "ensure logged in" step at batch start.
- launchApp:
    clearState: true
- runFlow:
    file: "_helpers/login.yaml"
