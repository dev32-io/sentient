# Copy this to ha_observe_token (no extension) and replace with a real
# Home Assistant Long-Lived Access Token (LLAT) for the observer user.
# chmod 600 ha_observe_token
# The gateway reads this via the env var referenced in hermes.home_assistant_observer.token_env.
REPLACE_WITH_HA_LLAT