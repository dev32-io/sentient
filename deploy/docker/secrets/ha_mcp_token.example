# Copy this to ha_mcp_token (no extension) and replace with a real
# Home Assistant Long-Lived Access Token (LLAT) for the MCP tool user.
# chmod 600 ha_mcp_token
# The Hermes MCP container reads this to call HA REST/API endpoints.
REPLACE_WITH_HA_LLAT_SEPARATE