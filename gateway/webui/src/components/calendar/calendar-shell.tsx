/**
 * Compatibility barrel for the pre-encapsulation Calendar shell imports.
 * Product callers should use CalendarWorkspace and the named local composites.
 */
export * from "./calendar-workspace.tsx";
